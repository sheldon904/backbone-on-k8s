{{- if .Values.notifyMcp.enabled }}
{{- $name := printf "%s-notify-mcp" (include "backbone.fullname" .) }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $name }}
  namespace: {{ include "backbone.namespace" . }}
  labels:
    {{- include "backbone.labels" . | nindent 4 }}
    app.kubernetes.io/name: notify-mcp
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $name }}
  namespace: {{ include "backbone.namespace" . }}
  labels:
    {{- include "backbone.labels" . | nindent 4 }}
    app.kubernetes.io/name: notify-mcp
    app.kubernetes.io/component: mcp-tool-server
spec:
  replicas: {{ .Values.notifyMcp.replicas }}
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: notify-mcp
      app.kubernetes.io/instance: {{ .Release.Name }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        {{- include "backbone.labels" . | nindent 8 }}
        app.kubernetes.io/name: notify-mcp
        {{- with .Values.podLabels }}{{ toYaml . | nindent 8 }}{{- end }}
      annotations:
        {{- if .Values.monitoring.scrapeAnnotations }}
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.notifyMcp.port | quote }}
        prometheus.io/path: "/metrics"
        {{- end }}
        {{- with .Values.podAnnotations }}{{ toYaml . | nindent 8 }}{{- end }}
    spec:
      serviceAccountName: {{ $name }}
      automountServiceAccountToken: false
      securityContext:
        {{- include "backbone.podSecurityContext" .Values.notifyMcp.securityContext | nindent 8 }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets: {{ toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{ toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{ toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.notifyMcp.topologySpread.enabled }}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: {{ .Values.notifyMcp.topologySpread.whenUnsatisfiable }}
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: notify-mcp
              app.kubernetes.io/instance: {{ .Release.Name }}
      {{- end }}
      containers:
        - name: notify-mcp
          image: {{ include "backbone.image" (list . .Values.notifyMcp.image) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.notifyMcp.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.notifyMcp.port | quote }}
            {{- if .Values.notifyMcp.audit.enabled }}
            - name: BACKBONE_AUDIT_DIR
              value: {{ .Values.notifyMcp.audit.mountPath | quote }}
            {{- end }}
            - name: BACKBONE_NTFY_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "backbone.fullname" . }}-config
                  key: ntfy_base_url
            - name: BACKBONE_NTFY_SHORTCUT_NAME
              valueFrom:
                configMapKeyRef:
                  name: {{ include "backbone.fullname" . }}-config
                  key: ntfy_shortcut_name
            {{- range $k, $v := dict "BACKBONE_NTFY_TOPIC" "ntfy_topic" "TELEGRAM_BOT_TOKEN" "telegram_bot_token" "TELEGRAM_HOME_CHANNEL" "telegram_home_channel" }}
            - name: {{ $k }}
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.secrets.existingSecret }}
                  key: {{ $v }}
            {{- end }}
          securityContext:
            {{- include "backbone.containerSecurityContext" dict | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            {{- if .Values.notifyMcp.audit.enabled }}
            - name: audit
              mountPath: {{ .Values.notifyMcp.audit.mountPath }}
            {{- end }}
          resources:
            {{- toYaml .Values.notifyMcp.resources | nindent 12 }}
          startupProbe:
            httpGet: { path: /healthz, port: http }
            periodSeconds: 1
            failureThreshold: 30
          livenessProbe:
            httpGet: { path: /healthz, port: http }
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            # /readyz, not /healthz: 503 when no delivery channel is configured,
            # so a misconfigured pod leaves the Service instead of answering 200
            # with {"ok":false}.
            httpGet: { path: /readyz, port: http }
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          lifecycle:
            preStop:
              # `sleep`, not `exec: [sleep, 5]` -- the image is distroless and
              # has no sleep binary. Lets the Endpoints controller deregister
              # this pod before it stops accepting.
              sleep:
                seconds: 5
      volumes:
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 16Mi
        {{- if .Values.notifyMcp.audit.enabled }}
        - name: audit
          persistentVolumeClaim:
            claimName: {{ $name }}-audit
        {{- end }}
      terminationGracePeriodSeconds: 30
{{- if .Values.notifyMcp.audit.enabled }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $name }}-audit
  namespace: {{ include "backbone.namespace" . }}
  labels:
    {{- include "backbone.labels" . | nindent 4 }}
  annotations:
    # The audit stream is evidence. Do not let `helm uninstall` delete it.
    helm.sh/resource-policy: keep
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: {{ .Values.notifyMcp.audit.size }}
  {{- with .Values.notifyMcp.audit.storageClass }}
  storageClassName: {{ . }}
  {{- end }}
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}
  namespace: {{ include "backbone.namespace" . }}
  labels:
    {{- include "backbone.labels" . | nindent 4 }}
    app.kubernetes.io/name: notify-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: notify-mcp
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
{{- if .Values.networkPolicy.enabled }}
---
# Prometheus must reach notify-mcp too. Fourth instance of the same trap: the
# exporter got a scrape allow rule and this did not, so Prometheus reported
# health=down on both replicas while the metrics endpoint was perfectly healthy.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ $name }}-scrape
  namespace: {{ include "backbone.namespace" . }}
  labels:
    {{- include "backbone.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: notify-mcp
      app.kubernetes.io/instance: {{ .Release.Name }}
  policyTypes: [Ingress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.monitoring.namespace }}
      ports:
        - protocol: TCP
          port: {{ .Values.notifyMcp.port }}
{{- end }}
{{- if .Values.notifyMcp.podDisruptionBudget.enabled }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ $name }}
  namespace: {{ include "backbone.namespace" . }}
  labels:
    {{- include "backbone.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.notifyMcp.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      app.kubernetes.io/name: notify-mcp
      app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
{{- end }}
